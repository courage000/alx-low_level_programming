#include <stdlib.h>
#include <time.h>
#include <time.h>

/**
 * main - annoying
 *
 * return: 0
 */
int main(void)
{
	int n;
	int x;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	x = n % 10:
	printf("last digit of %d is %d, n, x
	if (x > 5)
	{
	printf("and is greater than5")
	}
	if (x==0)
	{
	printf("and is 0");
	}
	if (x < 6 && x !=0)
	{
	print f("and is less than 6 and not 0");
	}
	printcf("\n");
	return (0);

}
